// import { GoogleGenerativeAI } from "@google/generative-ai";

// const genAI = new GoogleGenerativeAI(process.env.GOOGLE_API_KEY || "");

// // For text-only generation (fast, cheap)
// export const geminiNano = genAI.getGenerativeModel({ model: "gemini-2.5-flash-image-preview" });

// // For image + text processing
// export const geminiBanana = genAI.getGenerativeModel({ model: "gemini-2.5-flash-image-preview" });

